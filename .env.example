# Copy this file to .env and fill in your own key.
# Optional: the People Ops Agent runs fine without this, falling back to
# keyword-matched Q&A. Setting this key upgrades general Q&A to genuinely
# open-ended answers via the Claude API (Messages API).
ANTHROPIC_API_KEY=your-key-here
